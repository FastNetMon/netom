name: ci

on:
  push:
    branches:
      - main

  pull_request:
    branches: [main]

  workflow_dispatch:

env:
  CARGO_TERM_COLOR: always

permissions:
  contents: read

jobs:
  build:
    runs-on: ubuntu-24.04
    steps:
    - uses: actions/checkout@v7
      with:
        persist-credentials: false
    - name: Ensure correct Rust toolchain version
      uses: dtolnay/rust-toolchain@stable
    - name: Check build sources
      run: python3 pkg/check-build-sources.py
    - name: Test release planning
      run: python3 -B pkg/test-release-metadata.py
    - name: Build
      run: cargo build --locked --verbose
    - name: Run tests, including mrtgen MRT corpus
      run: cargo test --locked --lib --verbose
